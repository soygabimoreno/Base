package soy.gabimoreno.remoteconfig

// TODO: This is for taking it as an example
// val isPremiumSubscriptionLaunch =
//    remoteConfigProvider.isFeatureEnabled(RemoteConfigName.PREMIUM_SUBSCRIPTION_LAUNCH)

enum class RemoteConfigName {
    PREMIUM_SUBSCRIPTION_LAUNCH,
}
